server:
  port: 8083
  servlet:
    context-path: /inventory-service/
  tomcat:
    connection-timeout: 10000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/db_inventory
    username: postgres
    password: postgres
  application:
    name: inventory-service
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

seata:
  enabled: true
  application-id: inventory-service
  tx-service-group: demo-test
  service:
    vgroupMapping:
      demo-test: default
    grouplist:
      default: 127.0.0.1:8088
